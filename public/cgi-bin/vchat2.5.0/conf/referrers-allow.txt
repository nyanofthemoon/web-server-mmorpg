# List of referring URLs allowed access to the VolanoChat server.# The format is a Java properties file with each line containing:#     URL=Comment# where URL is the key and "Comment" is the value.  The "=Comment" value part# is ignored and may be omitted.  You must escape the colon character in the# URL by preceeding it with a backslash like the following examples:#     http\://www.volano.com/vcclient/chat.html=Any descriptive comment#     http\://www.volano.net/chat.html# The default below allows all referring Web addresses to connect.http\: