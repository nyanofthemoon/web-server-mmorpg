# List of host IP addresses allowed access to the VolanoChat server.# The format is a Java properties file with each line containing:#     IP=Comment# where IP is the key and "Comment" is the value.  The "=Comment" value part# is ignored and may be omitted.  IP addresses are in the IPv4 format:#     a.b.c.d# with values 0 to 255.  Zeros on the right signify ranges of network addresses.# The default of "0.0.0.0" allows all IP addresses to connect.0.0.0.0